{% extends "base.twig" %}

{% block content %}
<div class="container">
	{% for post in posts %}
	<div class="article-test">
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
	</div>
	{% endfor %}
</div>
{% endblock %}
